// Named arguments work on overloaded functions: the name set filters candidates, then types pick.

fun h(x: int): int { return x; }
fun h(x: string): int { return 1; }

fun main(): void {
    System.println(h(x: 5));
}
