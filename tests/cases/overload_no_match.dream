// No overload accepts a bool argument -> no-match error.
import system;
fun h(x: int): int { return 1; }
fun h(x: string): int { return 2; }

fun main(): void {
    let b: bool = true;
    System.println(h(b));
}
