// `internal` (module_utils_math's shared_helper) is reachable from another file that declares
// the same module, via module_internal_partner.combine().
import helpers.module_utils_math;
import helpers.module_internal_partner;

import utils.math.combine as run_combine;

fun main(): void {
    System.println(run_combine());
}
