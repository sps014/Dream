// `Buffer.realloc`/`Buffer.free` are `@unsafe`: calling them from a non-`@unsafe` function is a
// compile-time error.

import system;
fun main(): void {
    let xs = Buffer.alloc<int>(3);
    xs = Buffer.realloc<int>(xs, 5);
    System.println(xs.size());
}
