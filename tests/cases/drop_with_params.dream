// A destructor must not declare parameters.
import system;
class Bad {
    id: int;

    del(extra: int) {
        System.println(this.id);
    }
}

fun main() {
    let b = Bad(1);
    System.println(b.id);
}
