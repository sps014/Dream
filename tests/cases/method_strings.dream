// Regression test: string literals inside method bodies and passed as method
// arguments must be collected into the data segment (previously panicked at codegen).
import system;
class Greeter {
    public name: string;

    public constructor(name: string) {
        this.name = name;
    }

    public fun greet() {
        System.print("hello from method body\n");
    }

    public fun say(msg: string) {
        System.print(msg);
    }
}

fun main() {
    let g = Greeter("ignored");
    g.greet();
    g.say("string passed to method\n");
}
