import system.gpu;

@compute(64)
fun kernel(out: GpuBuffer<float>): void {
    out[0] = 1.0;
}

fun main(): void {
    // Must not call a @compute kernel like a CPU function.
    kernel(GpuBuffer<float>.alloc(1));
}
