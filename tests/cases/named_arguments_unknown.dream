// A named argument that doesn't match any declared parameter -> error.
import system;
fun greet(name: string): string {
    return "Hello, " + name;
}

fun main(): void {
    System.println(greet(nam: "Ada"));
}
