// Unboxing an `object` as the wrong primitive type must halt with a diagnosable message rather
// than silently reinterpreting the wrong bytes.
fun main(): void {
    let o: object = "hello";
    System.println("before");
    System.println((int)o);
    System.println("after");
}
