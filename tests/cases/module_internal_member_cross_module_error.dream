// This file has no `module` declaration (unnamed root module), so accessing the `internal`
// field `x` of a `Vector` declared in module `member.mod` is an error.
import helpers.module_internal_member;

fun main(): void {
    let v: Vector = make_vector(5);
    System.println(v.x);
}
