// With annotated parameters, a lambda's return type is inferred from its body — no fun(...) annotation needed.

import system;
fun main(): void {
    let f = (x: int) => x * 2;
    System.println(f(21));
}
