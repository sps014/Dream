// `@json` only applies to classes, structs, and discriminated unions, not free functions.
import system;
import system.json;
@json
fun greet(): string {
    return "hi";
}

fun main() {
    System.print(greet());
}
