// `@js` requires exactly two string arguments: the import module and field name.
import system;
class Bridge {
    @js("only_module")
    static extern fun read(): string;
}

fun main() {
    System.print(Bridge.read());
}
