// A Set literal has no bare-element fallback type (unlike `[...]`, which falls back to `T[]`), so
// it requires a `Set<T>`-typed context -> error.
fun main(): void {
    let s = {1, 2, 3};
    System.println(s);
}
