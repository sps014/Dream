import system;
import html;

// Demo: `html { }` markup expanded at compile time by the sample Dream parser.

fun page(title: string): string {
    return html {
        <div class="hero">
            <h1>{title}</h1>
            <p>Welcome</p>
        </div>
    };
}

fun main() {
    System.println(page("Hello"));
}
